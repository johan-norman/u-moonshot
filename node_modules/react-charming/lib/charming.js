import * as React from 'react';
import runes from 'runes';
class Charming extends React.Component {
    render() {
        const { className, letters, render, tagName } = this.props;
        const ltrs = runes(letters);
        const letterViews = ltrs.map((ltr, idx) => {
            return (React.createElement("span", { key: idx, className: `${className}-${idx}`, "aria-hidden": true }, ltr));
        });
        return React.createElement(tagName, { "aria-label": letters, className: className }, render(letterViews));
    }
}
Charming.defaultProps = {
    className: 'Charming',
    letters: '',
    tagName: 'div'
};
export default Charming;
